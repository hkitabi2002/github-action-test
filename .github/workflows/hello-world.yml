name: hello-world

on: push

jobs:
  hello-world-job:

    runs-on: ubuntu-latest

    steps:
       - name: check out repo code
         uses: action/checkout@v4
       - run: echo "$(cat hello_world.txt)"
